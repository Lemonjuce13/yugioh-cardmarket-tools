"""Postik — print address & postage labels onto sticker sheets.

Turns recipient addresses + postage into a print-ready DIN A4 sticker-sheet PDF for small
sellers who ship in envelopes. UI-agnostic engine (`core`, `io`) plus thin frontends
(`cli`, `api`). See the README for the architecture and the future-app roadmap.
"""

__version__ = "0.1.0"
